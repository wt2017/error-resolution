#1 add below registry mirrors, a good mirror resource for docker.io !
❯ cat /etc/docker/daemon.json 
{
  "registry-mirrors": ["https://docker.1ms.run"]
}

#2 restart docker
sudo systemctl daemon-reload
sudo systemctl restart docker
