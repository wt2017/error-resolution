export GOPROXY=https://goproxy.cn,direct && [compilation command]

# real case below
❯ git diff
diff --git a/Dockerfile b/Dockerfile
index 358f976be..258fb1ac5 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -11,6 +11,9 @@ COPY api/nfd/go.mod api/nfd/go.sum /go/node-feature-discovery/api/nfd/
 
 WORKDIR /go/node-feature-discovery
 
+# Use Chinese Go proxy to avoid timeout issues
+ENV GOPROXY=https://goproxy.cn,direct
+
 RUN --mount=type=cache,target=/go/pkg/mod/ \
     go mod download

